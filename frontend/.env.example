# DefaultSense AI — Frontend env (copy to .env for overrides)
# In dev, leave this unset: Vite proxies /api → http://localhost:8000 (see vite.config.js).
# For a deployed backend, set the full API base URL:
# VITE_API_URL=https://api.your-domain.com/api/v1
